{
    "ssl": {
        "keyPath": ".pem from iot connect device > connection info > certificate download button",
        "certPath": ".crt from iot connect device > connection info > certificate download button",
        "caPath": ".pem from settings > key vault > select sdk > download as .pem"
    },
    "ids": {
        "uniqueId": "unique device id",
        "sid": "found in organziation key vault under sdk version"
    },
    "networking": {
        "discoveryUrl": "found in organization key vault"
    }
}
